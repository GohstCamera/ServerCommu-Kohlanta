command /gms [<player>]:
    permission: admin.gamemode.survival.toperson
    permission message: &cVous n'avez pas la permission d'utiliser cette commande.
    trigger:
        if arg-1 is not set:  # Si aucun argument (pseudo) n'est donné
            set the gamemode of player to survival
            send "&aVous êtes maintenant en mode survie !" to player
        else:  # Si un pseudo est donné
            if arg-1 is a player:  # Vérifie que le pseudo correspond à un joueur en ligne
                execute console command "gamemode survival %arg-1%"
                send "&aLe joueur %arg-1% a été mis en mode survie !" to player
                send "&aVous avez été mis en mode survie par %player%." to arg-1
            else:
                send "&cLe joueur spécifié n'est pas en ligne ou n'existe pas !" to player
